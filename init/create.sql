CREATE TABLE `test1`.`users` (
  `id` VARCHAR(255) NOT NULL,
  `name` VARCHAR(255) NULL,
  PRIMARY KEY (`id`));

CREATE TABLE `test1`.`books` (
  `id` VARCHAR(255) NOT NULL,
  `name` VARCHAR(255) NULL,
  `author` VARCHAR(255) NULL,
  PRIMARY KEY (`id`));
